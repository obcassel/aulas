// Comando de preprocessor - incluir stdio.h antes de compilar o código
#include <stdio.h> 

// Função especial, define onde o programa começa sua execução
int main() {
    // Função que faz que uma mensagem seja mostrada na saída padrão
    printf("Aprovado na ESFCEx!");
    // Determina o fim de um bloco de controle (main) e retorna o valor zero
    return 0;
}

