
/* Atenção o código não será compilado, pois não possui uma função main */
/* O código serve apenas para mostrar exemplos de erros na linguagem C */

    // ERRO SINTATICO
    a = 1 // Sem ponto-e-vírgula
    d = a + b - c *   ; // Sem número para multiplicação
    
    // Variável declarada:
    int x, y; 
    // Var. declarada e inicializada:
    int contador = 0;
    float base = 2.5;  
    